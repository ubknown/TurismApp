# ✅ TurismApp Environment Configuration Template - UPDATED FOR SECURITY
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!

# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/turismdb
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

# Email Configuration (Enhanced)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_email_app_password_here
EMAIL_DEBUG=false
EMAIL_SMTP_TRUST=smtp.gmail.com

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here_at_least_32_characters
JWT_EXPIRATION=86400000

# Application URLs
APP_BASE_URL=http://localhost:8080

# Development vs Production
SPRING_PROFILES_ACTIVE=dev

# CRITICAL SECURITY NOTES:
# ✅ NEVER use default passwords in production
# ✅ Use App Passwords for Gmail, not regular passwords
# ✅ Generate JWT secret with: openssl rand -base64 32
# ✅ For production: use environment variables, not .env files
# ✅ Ensure .env is in .gitignore
